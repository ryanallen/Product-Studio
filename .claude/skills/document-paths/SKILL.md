---
name: document-paths
description: Update work/paths.md path tree with new paths and remove paths that no longer exist. Then run step 3 of the Save workflow in Coordinator.
---

# Document Paths

1. Compare actual paths under `work/` to the tree in `work/paths.md`.
2. Update `work/paths.md`: add new paths that exist on disk; remove tree entries that no longer exist.
3. Keep format (see paths.md).
4. When done, run step 3 of the **Save** workflow in [Coordinator](../../agents/coordinator.md).
